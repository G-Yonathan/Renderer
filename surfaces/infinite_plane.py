from shape import Shape


class Plane(Shape):
    def __init__(self, normal, offset, mat_idx):
        self.normal = normal
        self.offset = offset
        self.mat_idx = mat_idx

    def find_itersection(ray):
        return super().find_itersection()
