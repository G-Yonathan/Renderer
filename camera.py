class Camera:

    def __init__(position, look_at, up, sc_dist, sc_width):
        pass

    def ray_generator():
        for i in range(screen_width):
            for j in range(screen_length):
                # Calculate ray

                yield ray